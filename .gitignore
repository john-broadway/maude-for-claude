__pycache__/
*.pyc
*.egg-info/
dist/
build/
.venv/
.pytest_cache/
.ruff_cache/
htmlcov/
*.db
.mypy_cache/
*.env
!template/**/*.env
*.key
*.pem
*.p12
secrets.yaml
credentials.yaml
.credentials/

# Local Claude Code settings + co-installed plugin working dirs
.claude/settings.local.json
.claude/worktrees/
.superpowers/

# Workspace runtime dirs — never part of the plugin (guard against an accidental `git add -A`)
.remember/
.scratch/

# Infra-bearing WIP design drafts — kept LOCAL until scrubbed. This tape spec's §7 is a live
# audit of the author's home (shorthand octets .90/.173/.32, a CT id) that the pre-push guard's
# full-IP regex can't catch. Structural block against an accidental add; scrub before it ships.
docs/specs/2026-07-17-maude-memory-the-tape.md
