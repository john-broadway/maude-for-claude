"""maude_tape — the tape.

Claude wakes each session with amnesia; the tape is the video that brings him back to the
user, whole and current, with nothing cut (the 50 First Dates frame).

A tiered, loop-driven memory: upgradeable, degradable, cleanest on transactions. This package
holds the primary durable state — the user's verbatim words, rejected phrasings, and the
supersession history — that a disposable markdown index cannot.
"""
